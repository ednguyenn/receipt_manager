TODOlist
+ refactor lambda function, only save raw text + receipt_id into dynamodb 
+ query dynamodb table to match the search data
+ dockerize the Flask application 
+ deploy flask app on ECS and ECR 